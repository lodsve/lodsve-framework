<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="message.workflow.repository.FormUrlRepository">
    <insert id="saveFormUrls" parameterType="List">
        insert into t_form_url (id, url, type, flow_id)
        values
        <foreach collection="formUrls" open="(" close=")" item="formUrl" separator=",">
            #{formUrl.id}, #{formUrl.url}, #{formUrl.type}, #{formUrl.flow_id}
        </foreach>
    </insert>
</mapper>