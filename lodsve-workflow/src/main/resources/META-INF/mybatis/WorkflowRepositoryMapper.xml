<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="message.workflow.repository.WorkflowRepository">
    <select id="loadLatest" resultType="message.workflow.domain.Workflow" parameterType="Long">
        SELECT * FROM t_workflow t WHERE t.name = #{name} ORDER BY t.version DESC limit 1
    </select>

    <select id="findByDomain" resultType="message.workflow.domain.Workflow" parameterType="String">
        SELECT * FROM t_workflow WHERE domain = #{domain}
    </select>
</mapper>